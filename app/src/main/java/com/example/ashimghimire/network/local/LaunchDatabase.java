package com.example.ashimghimire.network.local;

import com.raizlabs.android.dbflow.annotation.Database;

@Database(name = LaunchDatabase.NAME, version = LaunchDatabase.VERSION)
public class LaunchDatabase {
    public static final String NAME = "LaunchDatabaseDB";

    public static final int VERSION = 1;
}
